[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "travel-schemas"
version = "0.1.0"
dependencies = ["pydantic>=2.6"]

[tool.setuptools.packages.find]
where = ["."]
include = ["travel_schemas*"]

