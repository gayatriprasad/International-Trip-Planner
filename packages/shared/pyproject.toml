[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "travel-shared"
version = "0.1.0"
dependencies = ["structlog>=24.1", "redis>=5.0.0"]

[tool.setuptools.packages.find]
where = ["."]
include = ["shared*"]
